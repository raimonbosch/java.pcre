This project is intented to be a simplification for using regular expressions in Java.

The code is inspirated by the pcre library http://php.net/manual/en/book.pcre.php written in php with some code optimizations only aplicable in Java language.